import suffixtree
import SuffixTrie as trie
import time
import random

# characters = ['A', 'C', 'G', 'T']

# # Generate 30 sequences each around 100,000 characters long
# sequences = [''.join(random.choice(characters) for _ in range(10000000)) for _ in range(1)]


# seconds = open("seconds.txt", "w")
# for seq in sequences:
#    start = time.time()
#    tree, pst = suffixtree.build(seq, regularize=True)
#    end = time.time()
#    seconds.write(str(end - start) + "\n")

# print("done")
